{
  "name": "cv-olusturucu-functions",
  "version": "1.0.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "express": "^4.18.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
